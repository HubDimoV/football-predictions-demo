code, text, payload = api_get("/fixtures", params={"date": "2026-07-16", "timezone": "Europe/Sofia"})
st.write("STATUS:", code)
st.write("TEXT:", text[:1000])
st.write("PAYLOAD KEYS:", list(payload.keys()) if isinstance(payload, dict) else None)
st.write("RESPONSE COUNT:", len(payload.get("response", [])) if isinstance(payload, dict) and "response" in payload else None)
st.json(payload if isinstance(payload, dict) else {"raw": text[:1000]})
